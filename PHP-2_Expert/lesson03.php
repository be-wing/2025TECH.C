<?php
declare(strict_types=1);
/* 
以下の仕様を満たす User クラスを記述してください。

プロパティ
    - private string $name
    - private int $age

コンストラクタ (__construct)
    - 引数として「名前」と「年齢」の2つを受け取ること。
    - 受け取った値を、それぞれのプロパティに代入すること。

実行時に "ユーザー「(名前)」を登録しました。" と表示すること。

メソッド
    public function profile(): void
    処理内容："(名前)さんは(年齢)歳です。" と表示すること。

3. 実行テスト（ファイル下部に記述）
名前 "田中"、年齢 25 でインスタンスを生成してください。
profile() メソッドを呼び出してください。
*/

class User {
    // 1. プロパティを定義 (private, 型指定あり)

    // 2. コンストラクタを定義 (__construct)

    // 3. プロフィール表示メソッドを定義

}

// 4. インスタンス化して動作確認
